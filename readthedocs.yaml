# readthedocs.yaml

version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "mambaforge-4.10"


branches:
  - main  # If the default branch is `main`


python:
  install:
    - method: pip
      path: .

conda:
  environment: docs/requirements.yaml
